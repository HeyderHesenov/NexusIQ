[pytest]
asyncio_mode = auto
testpaths = tests
# `app` paketi backend/ kökündədir — konsol-skript `pytest` (python -m olmadan)
# də onu tapsın deyə cwd sys.path-a əlavə olunur.
pythonpath = .
filterwarnings =
    ignore::DeprecationWarning
