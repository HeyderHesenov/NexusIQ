# ---- Web framework ----
fastapi==0.115.6
uvicorn[standard]==0.34.0
python-multipart==0.0.20

# ---- Settings / validation ----
pydantic==2.10.4
pydantic-settings==2.7.1

# ---- Database (async) ----
sqlalchemy==2.0.36
asyncpg==0.31.0
alembic==1.14.0
greenlet==3.1.1

# ---- News ingestion ----
feedparser==6.0.11
httpx==0.28.1
python-dateutil==2.9.0.post0

# ---- Language / AI ----
openai==1.59.6
anthropic==0.42.0

# ---- Analytics / correlation ----
pandas==2.2.3
numpy==2.2.1
scipy==1.15.0
yfinance==1.4.1

# ---- Scheduler ----
apscheduler==3.11.0

# ---- Utils ----
python-dotenv==1.0.1
pywebpush==2.3.0
Pillow==12.3.0  # thumbnail proksisi (routes/img.py) — naşir şəklini kiçildir

# ---- Tranzitiv, amma təhlükəsizlik üçün əhəmiyyətli (məlumat üçün pinlənməyib) ----
# starlette   — fastapi 0.115.6 ilə gəlir (quraşdırılmış: 0.41.3). Middleware
#               yığını (CORS/TrustedHost/GZip) və ASGI davranışı ondadır.
# cryptography — pywebpush → py-vapid ilə gəlir (quraşdırılmış: 49.0.0).
